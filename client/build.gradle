bootRun {
    enabled = false
}

bootJar {
    enabled = false
}


jar {
    enabled = true
    archiveClassifier.set("")
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifact jar
        }
    }
}